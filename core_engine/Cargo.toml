[package]
name = "core_engine"
version = "0.1.0"
edition = "2024"

[features]
default = []
cuda = ["dep:cudarc"]

[dependencies]
arc-swap = "1.7"
rand = "0.8"
rayon = "1.10"
cudarc = { version = "0.11", features = ["driver", "nvrtc"], optional = true }
